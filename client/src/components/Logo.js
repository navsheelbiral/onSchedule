import logo from '../assets/images/logo.svg'

const Logo = () => {
  return (<h2 style={{color: '#2cb1bc', fontWeight: 'bold'}}>onSchedule</h2>)
  // <img src={logo} alt='onSchedule' className='logo' />
}

export default Logo;
