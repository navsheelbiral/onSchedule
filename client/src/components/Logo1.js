import logo from '../assets/images/logo.svg'

const Logo1 = () => {
  return (<h2 style={{color: '#2cb1bc', fontWeight: 'bold', marginLeft: '3.5rem'}}>onSchedule</h2>)
  // <img src={logo} alt='onSchedule' className='logo' />
}

export default Logo1;
